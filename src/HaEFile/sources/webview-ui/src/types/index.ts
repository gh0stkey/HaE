import type {
  RuleColor,
  ScanRule,
  ScanResult,
  Severity,
  ValidatorConfig,
  ScanSummary,
  RuleResultsPayload,
  MatchSummary,
  FileHit,
  MatchFilesPayload,
  ActiveFilePayload,
  ScanSessionState,
  ExtensionMessage,
} from '../../../src/types/shared';

export type {
  RuleColor,
  ScanRule,
  ScanResult,
  Severity,
  ValidatorConfig,
  ScanSummary,
  RuleResultsPayload,
  MatchSummary,
  FileHit,
  MatchFilesPayload,
  ActiveFilePayload,
  ScanSessionState,
  ExtensionMessage,
};

export type ViewMode = 'results' | 'settings';
